It is a metric of divergence between two distributions. It works like a statistical distance to measure how different two distributions behave. Is defined as:

$$
D_{KL} (P\;||\;Q) = \sum_{x \;\in\; X} P(x) \log \frac{P(x)}{Q(x)}
$$